
// KOD ALEKSANDRA SKUBAŁY

/*#include <iostream>

#include <math.h>

bool isPerfect(int number)
{
  ​​​​

  int sum = 1, divider = sqrt(number);

  for (int i = 2; i <= divider; i++)
  {
    ​​​​

    if (number % i == 0)
      sum += (i + number / i);
  }
  ​​​​

  if (number == pow(divider, 2))
    sum -= divider;

  return (number == sum) ? true : false;
}
​​​​

int main()

{
  ​​​​

  int n;

  std::cout << "Podaj liczbe: ";

  std::cin >> n;

  std::cout << (isPerect(n) ? "Liczba jest doskonala" : "Liczba nie jest doskonala");

  return 0;
}
​​​​*/